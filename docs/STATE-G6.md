# G6 Wet Core 状态

## 判定

PASS（G6 Basic Pigment Profile）

## 实现

- 新增 `WetCoreSimulator`：固定 `1/60s` 步长、最多 8 个子步；闭域平流、守恒扩散、解析吸收/蒸发与移动颜料到结合颜料的沉积。
- 水分、移动 RGB 颜料、结合 RGB 颜料和速度均分离保存；多次注水按水量加权合并速度。
- 新增 G6 严格单元测试与真机 `G6AcceptanceActivity`，证据写入应用外部文件目录。

## 验收结果

| 门禁 | 结果 |
| --- | --- |
| G6-01 封闭质量守恒（100/500/1000） | 误差均为 `0` |
| G6-02 平流质心 | `1.34e-5` cell |
| G6-03 CFL | `0.5` 安全；`1.5` 正确预警 |
| G6-04 扩散 | 质量残差 `1.00e-6`；峰值下降、方差上升、质心不移 |
| G6-05 吸收 | 最大解析误差 `4.47e-7`；严格单测同时验证 `R² ≥ 0.995` |
| G6-06 有限性 | negative / NaN / Inf 均为 `0` |
| G6-07 质量预算 | 水 `3.17e-7`，颜料 `5.56e-8` |
| G6-08 沉积 | mobile `1 → .09438`，bound `0 → .90562` |
| G6-09 湿画干 | 结合颜料增加，水/颜料预算残差均 < `1e-6` |
| G6-10 湿画湿 | 合并速度 `-0.25`，水量 `2` |
| G6-11 湿度 | 单调违例 `0` |

## 遗留

- 本阶段交付经过数值验收的湿媒介核心；将其接入实时画布和高级颜料光学属于后续渲染阶段。
